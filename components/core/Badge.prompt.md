Small mono count/status marker. Tones stay in the hue budget: blue (default), gold (reward/active), fading (needs review), neutral.

```jsx
<Badge tone="gold">12</Badge>
<Badge tone="fading">正变暗</Badge>
<Badge dot tone="gold" />   // notification dot
```

Use `dot` for the inbox/unread indicator on sidebar items.
