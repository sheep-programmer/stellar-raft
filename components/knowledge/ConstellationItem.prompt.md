Sidebar row for one constellation (note group) under 「我的星座」. The dot color carries the group's overall memory health (warm = solid, cold = dimming).

```jsx
<ConstellationItem name="量子力学" color="var(--gold)" count={48} active />
<ConstellationItem name="数据结构" color="var(--star-blue-dim)" count={12} />
```

Keep highlights to hairlines and faint fills — no solid color blocks.
